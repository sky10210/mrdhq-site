name: Patch Gallery Image Proxy

on:
  workflow_dispatch:
  push:
    paths:
      - 'scripts/patch_gallery_image_proxy.py'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - run: python scripts/patch_gallery_image_proxy.py
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add marketing/project-gallery/index.html
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Use Apps Script proxy for Gallery images"
            git push
          fi
